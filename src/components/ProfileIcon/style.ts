import styled from 'styled-components/native';
import { ProfileIconWrapperProps } from '.';

export const ProfileIconWrapper = styled.View`
    height: auto;
    width: auto;
    flex-direction: column;
    align-items: center;
`;